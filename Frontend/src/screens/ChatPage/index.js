import React from 'react';
import Messenger from '../../components/Messenger';

const ChatPage = () => {
    return (
        <div>
            <Messenger />
        </div>
    );
};

export default ChatPage;